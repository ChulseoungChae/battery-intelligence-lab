# experiment: session

일별 압축 없이 세션(또는 고해상도) 궤적으로 학습하는 실험 자리입니다.

```bash
python scripts/run.py prepare --exp session   # 구현 후
python scripts/run.py train   --exp session
python scripts/run.py plot    --exp session
```

산출물: `traj/`, `models/`, `figs/`
